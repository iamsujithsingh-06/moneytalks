<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ cloud-backup / device-transfer data extraction rules.

    MoneyTalks stores authentication tokens (Capacitor Preferences), the offline
    financial ledger (IndexedDB "moneytalks"), SMS-derived drafts and raw SMS
    (IndexedDB "moneytalks-sms", SharedPreferences "moneytalks_sms") and OCR
    receipts (IndexedDB "moneytalks-ocr"). All of this is sensitive, so every
    domain is excluded from cloud backup and device-to-device transfer.
    `android:allowBackup` is also false in the manifest for pre-Android-12.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
